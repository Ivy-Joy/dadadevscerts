<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Certificate</title>
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body { font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial; }
    .frame { border: 6px solid #F16522; padding: 36px; border-radius: 12px; max-width: 900px; margin: 0 auto; }
  </style>
</head>
<body class="bg-white p-8">
  <div class="frame bg-white">
    <div class="text-center">
      <img src="<%= logoPath %>" alt="Dada Devs Logo" style="height:60px; width:auto; margin:0 auto;" />
      <h1 class="text-3xl font-bold mt-4">Bitcoin Dada - Certificate of Completion</h1>
      <p class="mt-4 text-gray-600">This certifies that</p>
      <h2 class="text-5xl font-extrabold mt-2"><%= cert.name %></h2>
      <p class="mt-4 text-lg">has completed <strong><%= cert.cohort %></strong></p>
      <p class="mt-2 text-sm text-gray-500">Issued: <%= new Date(cert.issuedAt).toLocaleDateString() %></p>
    </div>

    <div class="mt-8 flex justify-center">
      <div class="text-center">
        <img src="<%= qrDataUrl %>" alt="QR" style="width:130px;height:130px"/>
        <p class="text-xs mt-2">Scan to verify</p>
      </div>
    </div>
  </div>
</body>
</html>
